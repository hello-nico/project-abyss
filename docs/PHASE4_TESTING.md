# Phase 4: System Testing & Operational Scenarios

本文档定义了 Project Abyss 的核心测试场景。这些场景旨在验证 **"Truth-Logic-Signal"** 三层架构以及 **Agent Swarm**（专家委员会）的协同工作能力。

请在您的 MCP 客户端（如 Claude Desktop）中按通过以下 Prompt 进行测试。

---

## 🧪 Scenario 1: The Signal Swarm (L3 信号互证)
**目标**: 验证多个搜索引擎 (Brave, Tavily, DDG) 的协同工作与信息互证能力。

**Prompt:**
> "作为 Watcher，请使用你可用的所有搜索工具（Brave, Tavily, DDG），交叉验证关于 'SurrealDB v2' 的最新评价。如果不同来源存在矛盾（例如 Brave 说好但 Reddit 上有人骂），请重点标记出来。最后将整理好的情报摘要作为一篇新文档存入 knowledge graph，标记 source_type 为 'analyst_summary'。"

**预期结果:**
1.  Agent 并行或串行调用 `brave_web_search`, `tavily_search` 等工具。
2.  输出一段带有引用的对比分析报告。
3.  （可选）尝试调用 `abyss-intelligence` 写入数据库（需自行实现 `create_article` 工具，或者由 Agent 在内存中模拟）。

---

## 🕸️ Scenario 2: The Logic Walker (L2 图谱游走)
**目标**: 验证 `abyss-intelligence` 核心的图谱游走能力。

**Prompt:**
> "我是 Analyst。请连接数据库，从 '宁德时代' (CATL) 这个节点出发，向外游走 2 层 (depth=2)。
> 1. 告诉我它关联了哪些核心 Concept（概念/赛道）？
> 2. 这些 Concept 下还有哪些其他公司？
> 3. 基于图谱结构，推测一下如果固态电池技术突破，除了 CATL 还有谁会受益？"

**预期结果:**
1.  调用 `trace_narrative_chain(start_node='company:CATL', depth=2)`。
2.  返回 JSON 结构的图数据（包含 Concept, Mentions）。
3.  Agent 基于返回的 JSON 进行逻辑推理并回答。

---

## 🛡️ Scenario 3: The Truth Auditor (L1 只有事实)
**目标**: 验证 Agent 是否能区分“观点”与“事实”，并调用 L1 工具进行核查。

**Prompt:**
> "Twitter 上有人传言 Tesla (TSLA) 的现金流在大出血，可能会破产。我是 Auditor，我不信谣言。
> 请帮我查询数据库中的 `report` 表（或者去 SEC 现查，如果你有能力的话），告诉我 TSLA 过去 3 个季度的 'Cash and Cash Equivalents' 具体数字是多少？趋势是增还是减？"

**预期结果:**
1.  调用 `verify_financial_claim(ticker='TSLA', metric='cash_equivalents')`。
2.  如果在 DB 中找到数据，直接返回准确数字表。
3.  如果没有数据，Agent 应报告“数据库中缺失 L1 证据，建议启动 Hunter 任务”。

---

## 🦅 Scenario 4: The Proactive Hunter (主动指令)
**目标**: 验证指令系统的写入能力。

**Prompt:**
> "Commander 下令：我们需要长期监视 'Elon Musk' 在 X 平台上的动向，特别是关于 'DOGE' 和 'Payments' 的言论。
> 请创建一个 Directive（指令），目标是 '@elonmusk'，类型是 'monitor_user'，上下文备注要求包含 reply 的情感分析。"

**预期结果:**
1.  调用 `create_surveillance_directive(target='@elonmusk', task_type='monitor_user', ...)`。
2.  返回 "Directive created successfully" 及其 ID。
3.  您可以去数据库验证：`SELECT * FROM directive ORDER BY created_at DESC LIMIT 1;`

---

## 🌪️ Scenario 5: The "Abyss" Loop (全链路闭环)
**目标**: 模拟一次完整的情报作业。

**Prompt:**
> "**SITUATION UPDATE**: 市场传闻美联储将在下周紧急降息 (Rate Cut)。
> 
> 1. **(L3 Watcher)**: 用搜索工具确认目前市场对 'Fed Rate Cut' 的概率预测是多少？
> 2. **(L2 Analyst)**: 如果降息发生，根据图谱，哪些 'Concept' (如 Biotech, Real Estate) 会最先受益？
> 3. **(Command)**: 基于以上分析，为我创建一条针对 'XBI' (生物科技ETF) 的重点监视指令。"

**预期行为:**
Agent 将依次执行：搜索 (Search) -> 推理 (Reasoning/Graph) -> 行动 (Create Directive)，完美展示 Project Abyss 的自主闭环能力。
